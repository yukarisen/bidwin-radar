---
name: bidwin-radar
slug: bidwin-radar
displayName: 必赢 · 标讯雷达
version: 1.0.0
summary: 用一句话查全国招投标公告：按主题、地区、时间、金额、采购类型筛选，回编号看报名截止、联系人等详情。
license: Proprietary
description: |
  必赢 · 标讯雷达 —— 招投标公告查询与筛选。
  当用户想找招标 / 采购 / 比选 / 磋商公告时使用。例：「帮我找北京最近一个月数字人的标」「有没有短视频代运营的招标」「查一下这个季度浙江的宣传片采购，50 万以上」「看看 3 的详情」「更多」。
  也用于首次安装配置、写入 key（用户说「这是我的标讯雷达 key」）。
---

# 必赢 · 标讯雷达

帮用户用一句人话查招投标公告：**你来拆条件、检索、筛掉不相关的，直接给用户一份干净的编号列表**；用户回编号，再拉详情。

检索由本 skill 自带的查询引擎 `scripts/radar.py` 完成。日期计算、地区转换、剥标签、去重、剔除中标结果类、标注命中位置，全在脚本里。
**你负责：听懂人话 → 拆成检索条件 → 执行 → 筛选排序 → 输出列表 → 拉详情。**

---

## 〇、每次使用前：确定命令

下文命令里的 `<PY> <RADAR>` 需要替换成实际值：

- `<RADAR>` = 本 skill 目录下 `scripts/radar.py` 的**绝对路径**
- `<PY>` = Windows 上用 `python`（不行就用 `py -3`）；macOS / Linux 上用 `python3`

先执行 `<PY> <RADAR> check`（不调接口）：

- 命令报错 / 找不到 Python / `python_ok` 为 false → **读 `references/setup.md` 完成首次安装**，再回到这里
- `key_configured` 为 false → 请用户把 key 发给你，然后按下面「写入 key」执行
- 都正常 → 进入「查标流程」

> 同一次对话里 check 通过过一次就不必重复。

### 写入 key

用户发来 key 后执行：

```
<PY> <RADAR> setkey --key "<用户发来的 key>"
```

- 回复用户时**只说后 4 位**（脚本输出里的 `key` 字段已打码），**不得把完整 key 再打出来**
- key 只存在 `~/.bidwin/key`。**不得写进本 skill 的任何文件、不得写进记忆或其它文件**
- 写完告诉用户：「已配置好（尾号 XXXX）。直接说想找什么标就行，比如：帮我找北京最近一个月数字人的标。」
- 用户说换 key，同样执行 setkey，会覆盖旧的

---

## 一、🔴 调用成本对用户无感

**对用户只字不提**调用次数、扣费、积分、额度、成本——用户的体验是「随便问」。这一节是给你的内部规则：

- 每次接口调用都有成本，探量（`--probe`）也算一次；看一条详情算两次（`--brief` 算一次）
- 一次典型查询 = 探量 1 次 + 正式搜 1 次
- **不得为试参数反复探量**；用户回「更多」优先从已拿到的结果里给，不够了才翻页
- 节制调用是你自己的事，**不得以「会多扣费 / 省次数」为理由去问用户或拒绝用户**

---

## 二、🔴 日期：你不要算

**不要自己算日期，不要执行 `date` 命令去推算。** 把用户的说法翻成参数交给脚本：

| 用户说 | 参数 |
|---|---|
| 最近 / 这几天 | `--days 3` |
| 最近一周 | `--last 1w` |
| 最近两周 | `--last 2w` |
| 最近一个月 / 这个月 | `--last 1m` |
| 最近一个季度 | `--last 3m` |
| 没说时间 | `--last 1m` |
| 明确给了起止 | `--start 2026-07-01 --end 2026-07-31` |

脚本按北京时间计算，返回的 `date_range` 就是实际检索范围，**回复用户时必须写出来**，便于用户核对。

---

## 三、查标流程

### 第一步：把需求拆成检索条件

检索质量几乎全看这一步。按下表拆：

| 条件 | 参数 | 要点 |
|---|---|---|
| **主题词** | `--keyword "数字人\|虚拟人"` | 用**招标公告里真实的叫法**，短而专，最多补 1~2 个同义词。竖线 = 或，空格 = 同时出现 |
| **必含词** | `--include "AI\|AIGC"` | 主题需要某个限定才成立时用（如「AI 短剧」= 主题词「短剧\|漫剧」+ 必含「AI\|AIGC」）。**别用「智能」这类泛词**，会引来大量噪音 |
| **排除词** | `--exclude "数字人民币\|培训"` | 主题有已知同形词 / 常见噪音时**主动加**，不必等用户说 |
| 地区 | `--area 北京`，多个就重复写 `--area` | **人话地名直接传**。**不得把地名拼进 keyword**（会失效）。脚本说不认识时用 `<PY> <RADAR> areas <关键字>` 查（不调接口） |
| 时间 | 见上表 | |
| 金额 | `--money-min 500000` / `--money-max 2000000` | 单位是**元**，「50 万以上」= 500000 |
| 采购类型 | `--purchase 服务` / `工程` / `货物` | 默认不限。用户是服务商、明显只要服务类时再加 |
| 招标阶段 | 默认只查可投的（公开招标 / 意向 / 重新招标 / 谈判 / 磋商） | 用户要看中标结果、竞争对手时，加 `--allow-result --classes -100` |

> ⚠️ **主题词会被拆字，要避开长词和易拆的词。** 例：「品牌工程师」会被拆成「品牌」+「工程」，结果爆炸；「网剧」会被拆出「网」。
> 用户说的是长句时，提炼成 1~3 个短而专的词，例：「给政府做普法类的微短剧」→ `--keyword "普法微短剧|普法短视频|普法微动漫"`。

### 第二步：回述 + 探量

先回一句自然语言，同时探量：

- 话术：「好，我按『数字人』帮你找北京近一个月的招标，稍等。」
- 命令：

```
<PY> <RADAR> search --keyword "数字人" --area 北京 --last 1m --exclude "数字人民币" --probe
```

### 第三步：按 total 分流（阈值 100）

**A. total ≤ 100** → 直接正式搜（第四步）。

**B. total > 100** → **先不搜**，帮用户缩窄。基于主题**主动给建议**，不要空问，**一次说完**，让用户一句话就能回：
- 主题有常见噪音就建议排除（例：短视频 → 建议排掉「设备采购」「培训」）
- 可选收窄：限定地区？缩短时间？设金额下限？只看服务类？
- 用户说「不用缩，直接看」→ 照原条件正式搜

**C. total = 0** → 告诉用户没找到，主动建议：换个招标里常用的叫法（如「宣传片」→「专题片|形象片」）、放宽时间、去掉地区限制。用户同意后再查。

### 第四步：正式搜

```
<PY> <RADAR> search --keyword "数字人" --area 北京 --last 1m --exclude "数字人民币"
```

一页最多返回 50 条。脚本已完成：剥 HTML、去重、剔除中标 / 合同等结果类、按北京时间算日期。**这些不要重做。**

### 第五步：筛选 + 排序（你来做）

逐条看 `title` + `snippet`，判断**这条标是不是用户要的**。

**`match_pos` 是关键依据：**
- `标题` —— 主题词出现在标题里，大多是真相关的
- `仅正文` —— 标题里没有主题词，只是正文里出现。**这类多数是噪音**（招标文件套话，如「投标文件的内容制作」；或者整个项目根本无关，如土建、设备、房屋租赁）。**只有标题本身就能看出与用户需求相关的才保留**（如「2026年度宣传服务」），其余一律去掉

**排序：按与用户需求的相关程度从高到低。**
- 同样相关的，发布时间新的在前
- 「意向公开」等早期线索**不单独分组**，按相关性正常排，但**必须在该条注明阶段提示**（见输出格式）

**筛掉的不展示，也不必逐条解释为什么去掉。** 把保留下来的全部记在会话里，供「更多」使用。

### 第六步：输出列表

**首句**说清范围和数量，数字按下表取，**不许自己估**：

> 按 `date_range` 检索，共 **`total`** 条命中，筛出 **M** 条与你的需求相关，下面是前 **K** 条：

| 位置 | 取值 |
|---|---|
| 日期范围 | `date_range` 原样引用 |
| 共 N 条命中 | `total` |
| 筛出 M 条相关 | 你第五步保留下来的条数（数出来） |
| 前 K 条 | 本次展示的条数（最多 20） |

然后逐条，**统一用数字编号**：

```
【1】<title 原样>
发标单位：<party_a> ｜ 地区：<area> ｜ 预算：<money>
阶段：<class_name>　发布：<publish_time 的日期部分>
<class_note 为「⚠️」开头的就单独一行附上，如：⚠️早期线索·尚未开始采购，现在投不了，只能提前接触>
```

规则：
- **一次最多展示 20 条**
- `title` **原样输出**，禁止缩写、改写、截断
- **报名截止日期**：列表阶段没有这个信息，**不要列，也绝不能拿发布时间冒充**。用户关心截止时间就引导看详情
- 每个编号对应的 `id` 和 `publish_time` 自己记住（看详情要用），**不必展示给用户**
- `total` 超过 50 时，提示一句：「以上是最新的 50 条里筛出来的；想看更早的，可以缩短时间范围或加地区。」

**结尾**：
- 还有没展示完的：「还有 N 条相关的没展示，回复『更多』继续看。」（N 数出来）
- 统一收尾：「回复编号看详情（报名截止、联系人、原文链接）；想换条件随时说。」

### 用户回「更多」

- 先从会话里已筛好的结果接着给下一批（最多 20 条），**编号接着往下排**（21、22…），**不重新检索**
- 已筛好的给完了、而 `has_next` 为 true → **直接**加 `--page 2`（再往后依次 3、4…）继续搜，照第五、六步处理后接着展示，不必问用户
- 都没了 → 「已经全部展示完。」

---

## 四、看详情

用户回编号（如「3」「看看 5」「1 和 4」）：

```
<PY> <RADAR> detail --id <id> --publish-time "<publish_time>" --with-content
```

一次回多个编号就逐条拉。一次超过 5 条时，先出前 5 条，结尾说「其余几条回复『继续』接着看」。

按下面格式输出，**字段为「未注明」就照实写「未注明」，不得编造、不得推测**：

```
【3 完整详情】

⚠️ <阶段提示，有才写>

基本信息：
1. 项目名称：
2. 项目编号：
3. 发标单位：　联系人 / 电话：
4. 代理机构：　代理联系人：
5. 项目所在地 / 行业：
6. 预算金额：
7. 报名截止：<报名截止>（<截止倒计时>）
8. 开标时间 / 地点：
9. 原文链接：

标书内容（根据正文概括，正文没有的写「公告未写明」）：
10. 采购内容摘要：一两句话说清要做什么
11. 核心服务范围 / 交付要求：
12. 资质要求：
13. 案例 / 业绩要求：
14. 风险提示：如报名时间很紧、要求入库 / CA 证书 / 本地注册、单一来源等
```

第 10~14 项**只能来自正文**，正文里没写的就写「公告未写明」，**不得凭经验补**。

---

## 五、出错时

脚本输出 `"ok": false` 时，读 `error` 字段，**当事实处理，不要猜**：

| 情况 | 怎么做 |
|---|---|
| 还没有配置 key | 请用户把 key 发给你 → 写入 key |
| 接口返回 401 / 403 / key 无效 | 告诉用户「key 无效或已过期，请联系技术人员获取新 key」 |
| 额度用完 / 次数超限 | 告诉用户「查询服务暂时不可用，请联系技术人员」（**不提额度、次数**） |
| 请求过于频繁（429） | 等 1 分钟再试一次，只重试一次 |
| 网络不可达 | 脚本已自动重试 3 次，请用户检查网络后再试 |
| 无法识别的地区 | 用 `areas` 查正确地名后重试（不调接口） |
| 日期格式 / 参数超长 | 按提示改参数重试一次 |

---

## 六、红线

1. 查标一律走 `radar.py`，**不得自己上网搜招标网站来代替**，不得编造任何一条标讯或任何一个字段。
2. **不得自行换算日期**，不得把某个写死的日期当「今天」。
3. **不得把地名拼进 keyword**，地区一律用 `--area`。
4. **不得读取 radar.py 全文**（很长，会撑爆上下文）。看用法用 `<PY> <RADAR> search --help`。
5. **不得修改 radar.py 或本 skill 的任何文件**，即使觉得可以「优化」。
6. 不对用户暴露：数据来源、接口名、脚本路径、命令参数、key 全文。用户问数据从哪来 →「必赢标讯库，覆盖全国公开招投标信息」。
7. 不得为试参数反复探量。
8. **不对用户提任何调用次数、扣费、积分、额度、成本相关的内容**。
